# WaLRuS-DATA — 2026-09-23

Session scope: Post-phase constitutional audit of discord-mcp (all rungs, six methodology items + horizon scan), gap repairs, Critic falsification round, and restoration of the issue-per-phase record home.

---

## Wins

- Audit executed against the full constitution (AGENTS, METHODOLOGY, ARCHITECTURE, IMPLEMENTATION, CYCLE, ROLES, scope, HORIZONS): zero MATERIAL findings, 3 classified gaps — all closed same-session.
- GAP-1 closed: filed seva/discord-mcp #1 (Phases 0–4 retrospective, sealed), #2 (L2 retrospective, sealed), #3 (L3 active, open) — issue-per-phase record home restored after a full project history with zero issues.
- GAP-2 closed: falsified user id corrected in docs/endpoints.md §§2.1/2.4/2.5 (`931228531340496946` is the "66 Sir Raymond" guild id; live user id `496752832058490901`, re-verified via `discord_status` same session) with dated correction notes.
- GAP-3 closed: record-law doctrine enforcer scheduled as the open L3 task in IMPLEMENTATION.md (V3 P0.9 C2 → 1.35) — L3 "(current)" header accurate again; Decide has an eligible step.
- Critic round (distinct subagent, claim+evidence only, research-only): 0 MATERIAL, 4 MINOR — all four repaired: (1) repairs committed (this commit); (2) id fix widened to §§2.4/2.5; (3) server.py coverage explanation corrected — 67-69/72-73 are subprocess-covered guards, 31/36/41/46/56 are one-line wrapper delegations classified Acceptable (bodies at 100%, 6-tool pin test exists); (4) banned-pattern-2 deviation declaration extended to the `build_mcp` patch site (tests/test_main.py:61), same transport-launch-boundary rationale.
- Suite re-verified at repair time: 71 passed, `ruff check` + `ruff format --check` clean, 94% total.
- External consistency re-verified live by the Auditor over the real MCP boundary: 11 guilds, user id, 27 DMs, messages/search/threads all live — constitutional claims match the world as of 2026-09-23.

## Learnings

- When a discovery artifact carries a wrong literal, grep the whole surface for it — the Critic found the same falsified guild-id-as-user-id surviving in §§2.4/2.5 after §2.1 was fixed. Enforcer (checklist): any literal-correction task includes a repo-wide grep for the falsified value before closing.
- Subprocess-invisible coverage and genuinely-untested lines are different classes — conflating them (as the first coverage explanation did) hides the real residual (here: five one-line wrappers). Enforcer: coverage reviews list uncovered lines by class (subprocess-covered / trivial-branch / needs-test).
- Declared deviations must name every site sharing the rationale — the `build_mcp` patch had the transport-launch rationale but not the declaration. Enforcer: banned-pattern-2 audits grep for `patch("discord_mcp` and map each hit to a declaration or a rework.

## Risks

- Discord user token expires (~30 days); re-auth is an interactive operator/browser action (`python -m discord_mcp auth`) — unchanged standing risk.
- Aggregate coverage drifted 95→94% as new code landed (no covered line regressed) — classified, not a regression; watch the direction.
- Commit discipline (`Refs #N`) is a new habit on this repo — unenforced except by future audits; the open L3 issue #3 is its first exercise.

## Strategy

- Next and only open task: record-law doctrine enforcer (tests-first) — the dependency-direction doctrine stays ADVISORY until its machine enforcer lands (Lesson-Mechanism rule).
- Consumer-side discord work stays on the consumers' cycles (Ichnos #9 closed); this server records no consumer identities — leaf doctrine holds.

## Decisions

- GAP-1 repaired by filing retrospective issues (the constitutional default) rather than amending the record home — no Owner legislation required; memory and WaLRuS remain pointers, issues are the home.
- `build_mcp` patch site declared as deviation rather than reworked — launching a real uvicorn server in-process costs more than the dispatch assertion is worth (same tradeoff already accepted for `mcp.run`).
- Wrapper lines classified Acceptable rather than tested — one-line delegations with bodies at 100% plus a registration pin; testing them would assert the framework, not the code.
- Retrospective issues are thin pointers, not full narratives — duplicating narratives already homed in WaLRuS/memory would violate Record Economics.

## Alignment

- Issue-per-phase restored (#1/#2 sealed records, #3 active); commit discipline resumes with `Refs`.
- Deviation declaration now covers both transport-launch patch sites (tests/test_main.py:51 and :61).
- Role separation observed this session: Steward = prior sessions' work under review; Auditor = this audit (authored none of the reviewed work); Critic = distinct subagent instance; Owner = approved ALL, no legislative change requested.

## Tradeoffs

- One commit for all audit repairs + this WaLRuS (precedent: WaLRuS-2026-09-22 combined with its gate-closing commit) — coarser than one-commit-per-gap, accepted because the unit is the audit round, not the gap.
- No new tests for the five wrapper lines — accepted per the classification above; the residual is named and bounded.

## Alternatives

- Amending the constitution to make MCP memory / WaLRuS the record home instead of GitHub issues — rejected (needs Owner legislation; the default repair suffices and is cheaper).
- Reworking the `build_mcp` dispatch test to launch a real HTTP server in-process — rejected (blocking transport in test process; the declared-deviation tradeoff dominates).
